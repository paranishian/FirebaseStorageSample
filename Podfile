# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FirebaseStorageSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FirebaseStorageSample
  pod 'Firebase/Core'

  target 'FirebaseStorageSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FirebaseStorageSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
